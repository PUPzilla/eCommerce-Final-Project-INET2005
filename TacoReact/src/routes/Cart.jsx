import React from "react";
import Card from '../ui/card';
import { useState, useEffect } from "react";

export default function Cart() {
    
    const [cartProdID, setProdID] = useState(null);

    return(
        <>
        <h1>Cart Page</h1>
        </>
    );
};
